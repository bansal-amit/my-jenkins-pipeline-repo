pipeline {
    
    agent any
    stages {
        stage('Example'){
            steps{
                echo "${BUILD_NUMBER}"
                echo ${BRANCH_NAME}
            }
        }
        }           
    } //end of pipeline
